- name: user configure
  hosts: user
  become: yes
  tasks:
    - name: disable nodejs
      ansible.builtin.command: dnf module disable nodejs -y 

    - name: enable nodejs 20
      ansible.builtin.command: dnf module enable nodejs:20 -y

    - name: install nodejs
      ansible.builtin.command: dnf install nodejs -y

    - name: create system user
      ansible.builtin.user:
        name: roboshop
        system: true
        home: /app
        shell: /sbin/nologin
        comment: "Roboshop system user"

    - name: delete existing app directory
      ansible.builtin.file:
        path: /app
        state: absent

    - name: create app directory
      ansible.builtin.file:
        path: /app
        state: directory
    
    - name: delete user zip file
      ansible.builtin.file:
        path: /tmp/user.zip
        state: absent

    - name: download user app zip code
      ansible.builtin.get_url:
        url: https://roboshop-artifacts.s3.amazonaws.com/user-v3.zip 
        dest: /tmp/user.zip


    - name: umzip user code into /app directory
      ansible.builtin.unarchive:
        src: /tmp/user.zip
        dest: /app
        remote_src: yes


    - name: install dependencies
      community.general.npm:
        path: /app
    

    - name: copy user service # Jinja template
      ansible.builtin.template:
        src: user.service
        dest: /etc/systemd/system/user.service



    - name: start user
      ansible.builtin.service:
        name: user
        state: restarted
        enabled: yes